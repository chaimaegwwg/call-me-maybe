[project]
name = "call-me-maybe"
version = "0.1.0"
description = "AI project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.13.4",
    "torch>=2.13.0",
    "transformers>=5.13.1",
]
